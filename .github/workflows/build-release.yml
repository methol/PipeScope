name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    name: Build ${{ matrix.goos }}/${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/admin/package-lock.json

      - name: Install web dependencies
        working-directory: web/admin
        run: npm ci

      - name: Build web and sync embedded static assets
        run: make build-web sync-web

      - name: Verify embedded geo data assets exist
        run: |
          test -f internal/embeddeddata/assets/ip2region_v4.xdb.gz
          test -f internal/embeddeddata/assets/areacity_seed.csv.gz
          test -f internal/embeddeddata/assets/manifest.json

      - name: Build binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          mkdir -p dist
          go build -trimpath -o "dist/pipescope-${GOOS}-${GOARCH}" ./cmd/pipescope

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: pipescope-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/pipescope-${{ matrix.goos }}-${{ matrix.goarch }}
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all binaries
        uses: actions/download-artifact@v5
        with:
          pattern: pipescope-*
          path: dist
          merge-multiple: true

      - name: Create release and upload binaries
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2
        with:
          files: |
            dist/pipescope-linux-amd64
            dist/pipescope-linux-arm64
            dist/pipescope-darwin-amd64
            dist/pipescope-darwin-arm64
